@model IEnumerable<Blog20190528.Models.LookUp>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Category.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Post.postTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Category.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Post.postTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.lookupId }) |
            @Html.ActionLink("Details", "Details", new { id=item.lookupId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.lookupId })
        </td>
    </tr>
}

</table>
